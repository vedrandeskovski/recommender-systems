import LoadingComponent from "@/components/loading";
import { Spinner } from "@heroui/spinner";

const Loading = () => {
  return <LoadingComponent text="Loading movies from server..." />;
};

export default Loading;
